<div class="container">
  <div id="burgerHead" class="container">
    <img src="../assets/img/burger.png" class="rounded mx-auto d-block" alt="burger">
    <h1> Its Burger Time </h1> 
    <h2> are you ready</h2>
  </div>

  <div class="row">
    <div class="col-md-2"></div>
    <div id="toEat" class="col-md-4">
      <h2>To Eat!</h2>
      <ul class="list-group list-group-flush">
        {{#each burgers}}
        {{#unless devoured}}
        {{> burgers/burger-block-eat}}
        {{/unless}}
        {{/each}}
      </ul>
    </div>

    <div id="done" class="col-md-4">
      <h3>Done!</h3> 
      <ul class="list-group list-group-flush">
        {{#each burgers}}
        {{#if devoured}}
        {{> burgers/burger-block-delete}}
        {{/if}}
        {{/each}}
      </ul>
    </div>
    <div class="col-md-2"></div>
  </div>


  <div class="row">
    <div id="addBurg" class="col-med-12">

      <h2>Add a Burger</h2>
      <form class="create-form">

        <div class="form-group">
          <label for="bn">Burger:</label>
          <input type="text" id="bn" name="name">
        </div>

        <button type="submit"> Order Burger</button>
      </form>
    </div>
  </div>
</div>